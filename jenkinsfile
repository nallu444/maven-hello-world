pipeline { 
    agent any 
    tools {
        maven 'Maven3.2.1' 
          }
    stages { 
        stage('Build') { 
                           steps {
                                   sh 'mvn clean install'
                                 }
                       }
           }
         }

/* pipelinescript*/
